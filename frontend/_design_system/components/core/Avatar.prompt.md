Avatar — neutral circle of initials (or an image). Always slate, never amber.

```jsx
<Avatar name="Maria Souza" />
<Avatar name="Padaria Pão Quente" size="sm" />
```

Sizes: `sm` (28), `default` (36), `lg` (44). Pass `src` for a photo; otherwise initials are derived from `name`.
